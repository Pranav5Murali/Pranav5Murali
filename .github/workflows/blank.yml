name: Connect to Ubuntu VM using Password with Plink

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  ubuntu-ssh:
    runs-on: self-hosted
    environment: ubuntu1  # Use the environment where your secret is stored
    steps:
      - name: Check if environment secret is accessible
        env:
          SSH_PASSWORD: ${{ secrets.UBUNTU1 }}
        shell: pwsh
        run: |
          Write-Host "Checking if SSH_PASSWORD is set..."
          if ("$env:SSH_PASSWORD" -eq "") {
              Write-Host "❌ SSH_PASSWORD is empty!"
              exit 1
          } else {
              Write-Host "✅ SSH_PASSWORD is set."
              Write-Host "Password length: $($env:SSH_PASSWORD.Length)"
          }
      
      - name: Connect to Ubuntu VM using Plink
        env:
          SSH_USER: pranav
          SSH_PASSWORD: ${{ secrets.UBUNTU1 }}
          SSH_HOST: 192.168.1.100
        shell: pwsh
        run: |
          # Download Plink if not already installed
          if (-Not (Get-Command plink -ErrorAction SilentlyContinue)) {
              Invoke-WebRequest -Uri "https://the.earth.li/~sgtatham/putty/latest/w64/plink.exe" -OutFile "C:\Windows\System32\plink.exe"
          }
          # Use Plink to connect to the Ubuntu VM
          echo y | plink -ssh $env:SSH_USER@$env:SSH_HOST -pw $env:SSH_PASSWORD "ls -la /home/pranav/"
